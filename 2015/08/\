import sys

with open(sys.argv[1], "r") as f:
    contents = f.read().splitlines()

totchar = strchar = 0
for line in contents:
    totchar += eval(f"len({line})")
    strchar += eval(f"len(r'{line}')")

print(totchar, strchar, totchar - strchar)

